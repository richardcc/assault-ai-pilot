# Annex B - PDF Traceability (Rule -> Implementation Mapping)

## 1. Canonical Sources

- Core Rulebook: `2024_09_18_Rulebook_rev6_web.pdf`
- LOS Examples: `2024_10_LOS_Examples_v1.pdf`
- Terrain/Support Aid: `PN007_GameAid_Back_rev3_web.pdf`
- Terrain Clarification: `TEC_Clarification.pdf`
- Campaign: `ITA_Assault_Libro_Campagna_v1.0.pdf`
- Optional FoW: `2025_09_10_FoW_v01.pdf`

## 2. Mapping Matrix

| PDF section family | Documentation chapter | Engine concern |
| --- | --- | --- |
| Ch. 6-8 phase and action flow | `02_TURN_SEQUENCE_AND_ACTIONS.md` | phase machine, legal actions, activation |
| Ch. 9 movement and terrain | `03_MOVEMENT_AND_TERRAIN.md` | pathing, terrain costs, objective entry |
| Ch. 10 LOS/spotting/fire | `04_LOS_SPOTTING_AND_RANGED_FIRE.md` | LOS states, spotting gates, ranged dice |
| Ch. 11 close combat | `05_CLOSE_COMBAT_AND_CRITICALS.md` | close-combat rounds, critical routing |
| Ch. 12 TAS/OAS | `06_TAS_OAS_AND_TERRAIN_DAMAGE.md` | support sequence, blast, crater mutation |
| Campaign book | `07_GELA_CAMPAIGN.md` | persistent progression and branching |
| FoW module | `08_OPTIONAL_FOW_RULES.md` | contact markers, reveal, recon |

## 3. Review Procedure

When any PDF changes:

1. identify impacted chapter(s),
2. update corresponding markdown chapter(s),
3. validate code-path alignment,
4. log update date in roadmap backlog.

## 4. Coverage Status Semantics

Use this status vocabulary in roadmap/backlog:

- `implemented`: behavior exists and is test-covered,
- `implemented-partial`: behavior exists with known gaps,
- `documented-only`: behavior specified but not coded,
- `unknown`: not yet assessed.

## 5. Known Work Item

Create and maintain `docs/GAP_ANALYSIS.md` with:

- vendor requirement,
- current implementation status,
- gap severity (high/medium/low),
- owner and target milestone.

## 6. Runtime Reproducibility Mapping (Non-PDF)

Validation state: **Pending Validation**.

| Operational rule | File/function | Test/status |
| --- | --- | --- |
| Start each SB3 run from a clean transient model workspace | `assault_sim/train/train_sb3.py` -> `_cleanup_model_workspace()` and `main()` cleanup gate | Pending dedicated regression test (`train startup cleanup`) |
| `train_sb3` CLI help is side-effect free | `assault_sim/train/train_sb3.py` -> `argparse` gate in `main()` before SB3/env setup | Pending dedicated CLI regression test (`--help` must exit without starting train) |
| Run multi-seed eval in parallel with isolated per-seed outputs | `run_train_eval.ps1` -> `-ParallelEvalSeeds` and `-EvalParallelJobs` | Pending operational validation on `42/43/44` |
| CAPTURE guardrail overrides are explicitly traceable | `assault_sim/decision/option_executor.py` -> `_tag_action()` override fields | Covered by trace backward-compat tests; pending full episode validation |
| CAPTURE progress-vs-staging diagnostics and anti-lateralization guardrails are explicitly traceable | `assault_sim/decision/option_executor.py` -> `_best_capture_staging_move()` now includes VP-adjacent-ring distance shaping + enemy-pressure-aware scoring + stronger near-VP lateral-loop penalties, `_move_closer(capture_strict=True)`, near-VP fallbacks (`forced_attack_near_vp_staging`, `forced_attack_open_vp_window` now `<=3` and lane-opening targets adjacent to uncaptured VP), support-fire coupling (`attack_gate_support_open_lane`) and suppression of low-value non-lane high-adv CAPTURE attacks, post-opening follow-up advance + cooldown guardrail, VP-relevant-only relaxed fallback, per-unit CAPTURE focus lock (`_capture_focus_lock_by_unit`), per-unit near-VP no-step-in streak, opening-window anti-spam throttle (per-unit decision spacing), aggressive L3 CAPTURE force (`aggressive_l3_capture_force`), minimum L3 CAPTURE quota (`minimum_capture_intent_quota`), and `_tag_action()` fields; all gated by `capture_guardrails_enabled` from `assault_sim/config/train_config.json`; propagated via `training_env/evaluator/results_analyzer` | Pending validation on smoke eval (`l3_capture_forced_rate`, `l3_capture_force_reason_counts`, `post_open_window_followup_success_rate`, `capture_suspected_progress_miss_rate`, `objective_progress_move` share, fallback reason mix, VP focus stability) |
| SB3 eval strategy sampling parity with runtime | `assault_sim/evaluation/eval_sb3.py` -> `SB3EvalController.act` evaluates `StrategicIntent` per activation (turn lock removed) | Pending validation on smoke eval (`L3 policy distribution` shift, VP entry metrics) |
| CAPTURE-only diagnostic mode for L3 isolation | `assault_sim/config/train_config.json` -> `diagnostic_force_capture_only`; wired through `assault_sim/evaluation/eval_sb3.py`, `assault_sim/envs/gym_assault_env.py`, and `assault_sim/decision/option_executor.py` | Pending validation on smoke eval (`L3 CAPTURE share`, `vp_entries_taken`) |
| R4 step-in policy skeleton telemetry | `assault_sim/envs/gym_assault_env.py` (`_GymActionController.act`) + `assault_sim/evaluation/eval_sb3.py` (`SB3EvalController.act`) expose `stepin_legal_mask` / `stepin_forced_option`; propagated via `assault_sim/training_env.py` and aggregated in `assault_sim/evaluation/evaluator.py` / `assault_sim/evaluation/results_analyzer.py` | Closed as KILL in current cycle (`R4`/`R4.1` micro-gate failed); do not escalate to 120 episodes until a new single-lever hypothesis is defined |
| Runtime reaction fire hook (feature-flagged) | `assault_model/runtime/game_state_runtime.py` (`ASSAULT_ENABLE_REACTION_FIRE`) + reaction modules under `assault_model/combat/reaction_*` | Implemented-partial: default ON, deterministic first eligible reactor and per-reactor-per-turn cap; human-controlled reactor now opens `pending_reaction` window (manual use/skip decision path); unit tests in `assault_model/tests/test_runtime_reaction_fire_flag.py`; pending E2E trigger/interrupt/UI validation |
| VP-entry funnel observability (legal -> selected -> sustained control) | `assault_sim/decision/option_executor.py` step-in flags/reasons + `assault_sim/training_env.py` info propagation + `assault_sim/evaluation/evaluator.py` mission counters + `assault_sim/evaluation/results_analyzer.py` aggregates/percentiles + `assault_sim/evaluation/record_sb3_trace.py` trace fields | Pending validation on smoke eval (`vp_stepin_selection_rate`, `vp_stepin_block_reason_counts`, `vp_no_legal_stepin_near_count`, `vp_control_after_entry_turns_p50/p90`, per-unit entry success) |
| Gym controller avoids turn-wide intent lock | `assault_sim/envs/gym_assault_env.py` -> `_GymActionController.act` | Pending smoke validation (`seed=42`, `episodes=30`) |
| Policy-side CAPTURE priority under pending objectives | `assault_sim/envs/gym_assault_env.py` (`_GymActionController.act`) and `assault_sim/evaluation/eval_sb3.py` (`SB3EvalController.act`) force `StrategicIntent.CAPTURE` when objectives remain and no emergency | Pending smoke validation (`L3 CAPTURE share`, `vp_entry_missed_rate`, `capture_conversion_after_contact`) |
| CAPTURE near-VP anti-passivity fallback | `assault_sim/decision/option_executor_capture.py` -> `OptionExecutorCaptureMixin._capture_priority_action`: near VP (`<=3`) only accepts `objective_progress_move` (not lateral staging), and before final hold tries movement fallbacks (`forced_near_vp_no_hold_advance` / `forced_near_vp_no_hold_flank`) | Pending smoke validation (`fallback reason mix`, `vp_entry_missed_rate`, `capture_conversion_after_contact`) |
| VOEC simulator neutral episode contract (`new_episode`, `legal_actions`, `step`, `is_terminal`) for architecture-agnostic benchmarking | `voec_sim/core/simulator.py` -> `VOECSimulator` and `voec_sim/contracts/types.py` | Pending Validation: `voec_sim/tests/test_sim_determinism.py`, `test_legal_actions_contract.py`, `test_terminal_conditions_v1.py` |
| VOEC asset parity import (use existing scenarios/units, no synthetic redefinition in v1) | `voec_sim/assets_bridge/importers.py` (`load_catalogs`, `list_scenario_ids`) | Pending Validation: `voec_sim/tests/test_asset_parity_units.py`, `test_asset_parity_scenarios.py` |
| MuZero adapter boundary over VOEC (no assault_sim training legacy dependency) | `agents/muzero/adapter_voec.py` | Pending Validation: `agents/muzero/tests/test_train_smoke.py` |
| MuZero VP objective reward shaping from transition events | `agents/muzero/core/selfplay.py` -> `shaped_training_reward` uses `vp_captures` and per-side VP net delta; train config knobs in `agents/muzero/configs/muzero_config*.yaml` (`vp_capture_bonus_per_hex`, `vp_net_gain_bonus`, `vp_net_loss_penalty`) | Pending Validation: `agents/muzero/tests/test_reward_targets.py` + short A/B train run (`tracked_captured_avg`, `scenario_outcome_rates`) |
| MuZero unit capability encoding in CNN observation | `agents/muzero/core/selfplay.py` -> `observation_to_tensor` loads `assault_sim/assets/catalogs/unit_catalog.json` and emits ally/enemy capability planes (`mobility`, `direct/indirect range`, `attack power`, `defense power`); channel names in `agents/muzero/train/train_muzero.py::_channel_name` | Pending Validation: `metrics/observation_channels.json` inspection + train/eval delta on VP capture KPIs |
| MuZero VP opportunity/capture metrics consistency | `agents/muzero/core/selfplay.py` infers `legal_capture_options` from legal action destination `(q,r)` over VP ownership map; `agents/muzero/train/train_muzero.py` now counts objective-action evidence when side VP gain or `vp_captures` occurs (not only CAPTURE string markers) | Pending Validation: `metrics/units_sides.json::vp_summary` must show non-zero `capture_actions_total` and non-zero `objective_opportunity_conversion.opportunities` on runs with non-zero `vp_captures_total` |
| MuZero reaction fire decision ownership | `voec_sim/core/simulator.py` exposes pending reaction window as explicit legal actions (`OPPORTUNITY_FIRE` / `OPPORTUNITY_SKIP`) and resolves via `RuntimeGameState.resolve_pending_reaction(...)`; `agents/muzero/core/selfplay.py` excludes `OPPORTUNITY_SKIP` from attack-like option counts | Pending Validation: smoke run should show reaction decision actions in transitions and non-zero reaction-event usage when windows appear |
| MuZero reaction fire efficiency penalty | `agents/muzero/core/selfplay.py::shaped_training_reward` adds `reaction_fire_miss` component driven by `reaction_fire_miss_penalty` from `agents/muzero/configs/muzero_config*.yaml`, penalizing `OPPORTUNITY_FIRE` with zero damage/kills | Pending Validation: compare A/B smoke runs for lower `OPPORTUNITY_FIRE` miss share and higher objective-opportunity conversion (`diagnostics_summary.objective_opportunity_conversion.rate`) |
| MuZero opportunity-phase FIRE vs SKIP VP tradeoff shaping | `agents/muzero/core/selfplay.py::shaped_training_reward` adds `opportunity_fire_no_progress` (from `opportunity_fire_no_progress_penalty`) and `opportunity_skip_capture_preserve` (from `opportunity_skip_capture_preserve_bonus`) gated by `opportunity_vp_distance_threshold`; fields are propagated to decision diagnostics via `agents/muzero/train/train_muzero.py` and shown in `mlops/reporting/viewer.py` Decision Influence (`Why this action vs VP`) | Pending Validation: `agents/muzero/tests/test_reward_targets.py` (`test_shaped_reward_prefers_opportunity_skip_to_preserve_near_vp_capture`, `test_shaped_reward_keeps_opportunity_fire_when_tactically_strong`) + replay inspection that opportunity rows surface the new weighting fields |
| MuZero objective progress funnel observability | `agents/muzero/core/selfplay.py` computes nearest-uncaptured-VP distance features per transition (`objective_distance_before/after`, `objective_progress_delta`, `objective_had_opportunity`, `objective_converted`); `agents/muzero/train/train_muzero.py` aggregates `diagnostics_summary.objective_progress_funnel` globally and by side | Pending Validation: smoke AB3 should show non-trivial `progress_rate`/`conversion_rate` and allow identifying stall-heavy profiles without reward hardcoding |
| MuZero/EfficientZero objective opportunity conversion retune | `agents/muzero/core/selfplay.py::shaped_training_reward` keeps objective-opportunity shaping centralized; conservative tuning applied in `agents/muzero/configs/muzero_config.yaml` and `agents/efficientzero_v2/configs/efficientzero_v2_config*.yaml` (`objective_progress_bonus_per_hex`, `objective_no_progress_penalty`, `objective_no_progress_attack_penalty`, `vp_capture_bonus_per_hex`) | Pending Validation: `agents/muzero/tests/test_reward_targets.py` must keep pass on objective opportunity penalty/progress-relative tests, then run short train+bench and verify lift in `diagnostics_summary.objective_opportunity_conversion.rate` without policy/value instability |
| MuZero train/eval objective-distance parity + eval head completeness | Base objective math is centralized in `agents/muzero/objective_signals.py` (`objective_min_distance_snapshot`, `objective_step_signal`) and used by both `agents/muzero/core/selfplay.py` and `assault_bench/runner.py`; eval rows now serialize `objective_min_dist_before/after`, `objective_progress_delta`, `objective_best_vp_id`, `vp_distance_vector`, and `objective_had_opportunity` from distance+legal opportunities; viewer renders those fields in replay diagnostics (`mlops/reporting/viewer.py`) | Pending Validation: `agents/muzero/tests/test_objective_signals.py`, `assault_bench/tests/test_benchmark_smoke.py::test_eval_objective_head_not_partial_when_distance_present`, `mlops/tests/test_reporting_viewer_replay_filter.py::test_model_html_does_not_embed_legacy_objective_distance_gap_reason` |
| Eval objective opportunity metric semantics (primary vs immediate) | `assault_bench/runner.py` now defines primary aggregate opportunity from `objective_had_opportunity` (`xai_vp_capture_opportunity_steps` -> `eval_vp_capture_opportunity_steps`) and keeps strict immediate capture-window opportunities separate (`xai_vp_immediate_capture_opportunity_steps` -> `eval_vp_immediate_capture_opportunity_steps`); `mlops/reporting/viewer.py` renders both metrics and denominator counters explicitly | Pending Validation: `assault_bench/tests/test_benchmark_objective_opportunity_metrics.py`, `mlops/tests/test_reporting_viewer_replay_filter.py::test_model_html_eval_head_diagnostics_shows_interpretable_intentions_and_coverage` |
| MuZero objective auxiliary head (next-step progress classifier, class-balanced) | `agents/muzero/core/network.py` adds `prediction_objective`; `agents/muzero/train/trainer.py` computes masked BCE on objective opportunities (`objective_had_opportunity`) against progress target (`objective_progress_delta > 0`) with positive-class weighting (`objective_pos_weight`) and blends it with main MuZero loss via `train.objective_loss_weight`; `agents/muzero/train/train_muzero.py` logs `objective_loss` and persists objective-head config in run manifest | Pending Validation: smoke AB3 should show non-zero `objective_loss` with improved `objective_progress_funnel.progress_rate` and non-regressing timeout/win metrics |
| Objective/reward no-hardcode runtime parameterization + reporting tab | YAML defaults/merge in `agents/muzero/configs/config_loader.py` (`train.objective_signal`, `train.objective_head`, `train.objective_reporting`), runtime use in `agents/muzero/core/selfplay.py`, `agents/muzero/train/trainer.py`, `agents/efficientzero_v2/train/trainer.py`, `agents/muzero/train/train_muzero.py`, `agents/efficientzero_v2/train/train_engine.py`; reporting UI tab in `mlops/reporting/viewer.py` shows active values and precedence with legacy `N/A` fallback behavior | Pending Validation: `agents/muzero/tests/test_config_loader_objective_defaults.py`, `agents/muzero/tests/test_objective_signals.py::test_objective_signal_respects_opportunity_near_vp_threshold`, `agents/muzero/tests/test_trainer_objective_head.py::test_trainer_respects_objective_progress_positive_threshold`, `agents/efficientzero_v2/tests/test_train_engine_contracts.py::test_episode_phase29_respects_conversion_window_override`, `mlops/tests/test_reporting_viewer_replay_filter.py` (new Objective/Reward tab tests) |
| Campaign objective-outcome timing precedence (end only on annihilation/turn-limit, then evaluate VP table) | `assault_model/runtime/game_state_runtime.py` (`_check_match_end`): objective outcome evaluation moved behind terminal gating (`alive_sides<=1` or `max_turns`), preventing premature `objective_outcome_resolved` before valid match-end condition | Pending Validation: short selfplay/eval run must show no early objective-outcome termination with two sides alive and turn budget remaining |
| MuZero phase-2.9 reaction/assault promotion gate | `agents/muzero/train/train_muzero.py` writes `phase_2_9_train_kpis`; `agents/muzero/core/selfplay.py` emits `legal_reaction_options`; `assault_bench/runner.py` aggregates `phase_2_9_eval_kpis`, builds `phase_2_9_train_eval`, and evaluates `phase_2_9_promotion_gate` | Pending Validation: benchmark run with checkpoint should produce PASS/FAIL gate with populated reaction+assault metrics and explicit check reasons |
| Benchmark parity harness (same scenario/seeds for MuZero stub vs baseline) | `assault_bench/runner.py` | Pending Validation: `assault_bench/tests/test_benchmark_smoke.py` |
| CAPTURE near-VP attack gating by VP relevance | `assault_sim/decision/option_executor_capture.py` -> `OptionExecutorCaptureMixin._capture_priority_action`: when near VP and movement exists, suppress non-VP-relevant opportunistic attack fallbacks (`forced_attack_after_staging_loop`, `attack_gate_high_adv`/`attack_gate_near_vp_pressure`) and preserve VP-relevant attacks (`attack_gate_vp_target`, `attack_gate_defend_owned_vp`, `attack_gate_support_open_lane`) | Pending smoke validation (`attack_near_vp_instead_of_capture_rate`, fallback reason mix, `vp_entry_conversion_rate`) |
| CAPTURE step-in setup fallback near VP | `assault_sim/decision/option_executor_capture.py` -> `OptionExecutorCaptureMixin._best_stepin_setup_move` and `_capture_priority_action`: if no legal step-in and VP is near (`<=3`), prefer movement that lands in step-in setup geometry (`forced_stepin_setup_move`) before attack fallback; unconditional near-VP `forced_attack_near_vp_staging` path suppressed | Pending smoke validation (`vp_stepin_block_reason_counts.no_legal_stepin_near_vp`, `vp_entries_taken`, fallback reason mix) |
| CAPTURE setup validity gate (adjacent-only) | `assault_sim/decision/option_executor_capture.py` -> `OptionExecutorCaptureMixin._best_stepin_setup_move`: setup fallback is accepted only when resulting nearest-uncaptured-VP distance is `<=1`; rejects lateral false positives (`2->2`) | Pending smoke validation (trace quality of `forced_stepin_setup_move`, `no_legal_stepin_near_vp` trend) |
| CAPTURE unit selection bias by VP-entry potential | `assault_sim/envs/gym_assault_env.py` (`_GymActionController.select_best_unit`) and `assault_sim/evaluation/eval_sb3.py` (`SB3EvalController.select_best_unit`): when sampled strategy is CAPTURE, rank candidates by immediate step-in legality and nearest uncaptured VP distance, then pick within top-3 by unit slot; adaptive reliability penalty variant disabled after regression | Pending smoke validation (`unit_concentration_index`, `per_unit_vp_entry_attempts`, `vp_entry_conversion_rate`) |
| CAPTURE local interception unit bias for threatened owned VP | `assault_sim/envs/gym_assault_env.py` and `assault_sim/evaluation/eval_sb3.py`: `select_best_unit()` invokes `_defense_intercept_unit()` to pick closest local unit (<=3 hexes) when an owned VP is threatened at enemy distance <=1; no global strategy override | Pending smoke validation (`first_us_vp_loss_turn`, `unit_concentration_index`, global win/loss impact) |
| CAPTURE recently-lost VP retake priority (unit selection, disabled) | Retake-priority variant was tested in `assault_sim/envs/gym_assault_env.py` and `assault_sim/evaluation/eval_sb3.py` and then disabled after regression; CAPTURE unit selection uses baseline path | Pending redesign/validation before re-enabling (`time_to_retake_after_loss`, `captured_final_counts`, global win/loss impact) |
| CAPTURE anti-crowding objective focus | `assault_sim/decision/option_executor_capture.py` (`OptionExecutorCaptureMixin._objective_target_hex`): stronger ally-pressure penalty near VP target + extra crowding penalty when multiple allies are already near the same VP, to reduce single-objective jams | Pending smoke validation (`unit_concentration_index`, `per_unit_vp_entry_attempts`, `vp_stepin_block_reason_counts.no_legal_stepin_near_vp`) |
| Policy-side CAPTURE override only on concrete entry opportunities | `assault_sim/envs/gym_assault_env.py` (`_GymActionController.act`) and `assault_sim/evaluation/eval_sb3.py` (`SB3EvalController.act`) force CAPTURE only when objectives are pending, no emergency, and `stepin_legal` is true | Pending smoke validation (`forced_ratio`, VP-entry metrics, policy/executor alignment) |
| Reward shaping for VP step-in + retention | `assault_sim/rewards/progressive_reward.py` + `assault_sim/config/reward_config.py`/`reward_config.json`: add `vp_stepin_selected_bonus`, `vp_stepin_missed_near_penalty`, `vp_control_after_entry_bonus` to reinforce entry/hold behavior | Pending train/eval validation (`vp_stepin_selection_rate`, `vp_no_legal_stepin_near_count`, `vp_control_turns_share`) |
| R2.1-A reward retune (post-contact VP conversion) | `assault_sim/config/reward_config.py`/`reward_config.json`: increase `vp_stepin_selected_bonus`, `vp_control_after_entry_bonus`, `capture_vp_hold_streak_bonus`, and increase CAPTURE no-progress penalties (`capture_no_progress_penalty`, `capture_idle_no_progress_penalty`, `capture_staging_repeat_penalty`) to prioritize entry+retention over lateral loops | Pending train/eval validation (`vp_entries_taken`, `vp_entry_conversion_rate`, `capture_attempt_success_rate`, `captured_final_counts`) |
| Anti-retreat loop with pending objectives | `assault_sim/decision/option_executor.py`: under `PRESERVE`, fallback and assault role-bias prefer `ADVANCE` when uncaptured objectives remain; `assault_sim/envs/gym_assault_env.py` and `assault_sim/evaluation/eval_sb3.py`: CAPTURE override also activates under near-objective pressure (`nearest_vp_d <= 2`) | Pending train/eval validation (`RETREAT` share under objectives pending, `vp_contact_rate`, `first_vp_entry_turn`) |
| System pass advances activation order | `assault_model/runtime/game_state_runtime.py` (`RuntimeGameState.apply_action`): `WaitAction` triggers `next_activation()` even without attacker (`WAIT:SYSTEM`), preventing side lock and stale activation state in UI | Pending UI validation on Human-vs-AI turn transitions (marker reset and active-side alternation) |
| CAPTURE anti-deadlock rebalance (movement vs attack) | `assault_sim/decision/option_executor_capture.py` -> `_capture_priority_action`: constrain `forced_stepin_setup_move` to VP ring `2..3`; when near VP and staging streak persists, re-open VP-relevant/gated attacks (`forced_attack_near_vp_staging` + gated attack path) to prevent zero-damage collapse | Pending smoke validation (`fallback_to_attack_rate_in_capture`, `damage_ratio`, `capture_fallback_reason_counts`, VP-entry metrics) |
| RL damage/kill side attribution normalization | `assault_sim/training_env.py` -> `TrainingEnv.step` damage/kill buckets now use normalized side comparison (`actor_side_norm` vs `rl_side_norm`) for enum-like side values | Pending validation via `assault_sim/tests/test_training_env_damage_side_attribution.py` + smoke eval telemetry sanity |
| R2.1-b capture conversion retune (reward + CAPTURE near-VP gating) | `assault_sim/config/reward_config.json` + `assault_sim/rewards/progressive_reward.py`: stronger VP entry/progress/retention shaping and stronger CAPTURE fallback/no-progress penalties; `assault_sim/decision/option_executor_capture.py`: block near-VP forced/gated CAPTURE attacks when progress move is available | Pending validation on smoke retrain (`vp_entry_conversion_rate`, `capture_attempt_success_rate`, `fallback_to_attack_rate_in_capture`, `capture_fallback_reason_counts`) |
| R2.1-b retune-3 conversion rebalance + eval dedupe | `assault_sim/config/reward_config.json` + `assault_sim/rewards/progressive_reward.py`: reduce over-penalization on CAPTURE fallback/no-progress while increasing post-contact progress and objective delta pressure; `assault_sim/evaluation/eval_sb3.py`: dedupe repeated `scenario_schedule` entries by scenario id to prevent duplicate comparative summaries | Pending validation on smoke retrain (`capture_attempt_success_rate > 0`, `vp_entry_conversion_rate` lift, `loss_rate` non-regression, clean non-duplicated comparative summary) |
| R2.1-g single-lever CAPTURE priority | `assault_sim/config/reward_config.py` + `assault_sim/config/reward_config.json`: increase only `capture_strategy_bonus` (`0.60 -> 0.90`) to raise CAPTURE strategic mass without touching planner/guardrails/finalizer levers | Pending validation on multi-seed short run (`capture_attempt_success_rate > 0`, stable `loss_rate`, `vp_entry_conversion_rate >= 0.30`, higher CAPTURE share) |
| R2.1-h single-lever post-contact conversion bonus | `assault_sim/config/reward_config.py` + `assault_sim/config/reward_config.json`: increase only `capture_post_contact_progress_move_bonus` (`0.70 -> 1.00`) to strengthen post-contact objective progress decisions without changing planner/guardrails/finalizer levers | Pending validation on multi-seed short run (`capture_attempt_success_rate > 0`, `loss_rate < 0.8`, `vp_entry_conversion_rate >= 0.30`, higher `reward_component_means.capture_post_contact_progress_move_bonus`) |
| Planner role contract hardening (no-UNKNOWN operational roles) | `assault_sim/decision/role_mapper.py` centralizes role resolution (`resolve_role_with_reason`), `assault_sim/decision/option_executor.py` consumes centralized resolver for tag emission, and `assault_sim/training_env.py` infers role from resolver when action tags are missing | Pending validation on smoke eval (`plan_role_counts_stub.UNKNOWN` down, no regression in mission outcomes) |
| SB3 two-phase capture curriculum + fast eval loop | `assault_sim/config/train_config.json` (`scenario_schedule` split 300+700, `sb3_eval_freq=5000`, `sb3_eval_episodes=20`) + `assault_sim/train/train_sb3.py` phase-1 CAPTURE force env override + `assault_sim/envs/gym_assault_env.py` env-override reader for capture guardrail flags | Pending validation on tuning runs (phase-1 CAPTURE pressure lift, faster iteration cadence, no final-phase regression) |
| ATTRIT/DENY anti-collapse to movement loops | `assault_sim/decision/option_executor.py` -> `execute()` remaps `ADVANCE` to `ATTACK` for `ATTRIT`/`DENY` when immediate legal attack exists and not in strict near-VP CAPTURE budget context; `assault_sim/rewards/progressive_reward.py` + `assault_sim/config/reward_config.py/json` add L3/L2 coherence shaping (`l3_attrit_*`, `l3_deny_*`) | Pending smoke validation (`strategy -> option` attack share, `DIRECT_RANGED` usage, `damage_ratio`) |
| CAPTURE force de-escalation to reduce policy coercion | `assault_sim/decision/option_executor.py` -> `execute()` limits aggressive CAPTURE force and capture quota forcing to near-objective context (`nearest_vp_d <= 2`), constrains DENY->CAPTURE remap to no-immediate-attack cases, and scopes near-VP budget overrides to CAPTURE strategy only | Pending smoke validation (`ATTACK->ADVANCE` drop, `forced_ratio` drop, stable VP-entry metrics) |
| ATTACK fallback contract + observability | `assault_sim/decision/option_executor/combat.py` -> `_execute_attack(..., allow_move_fallback)` allows tactical reposition only when movement improves pressure/progress (`no_legal_attack_reposition`), otherwise emits hold fallback (`no_legal_attack_hold_no_progress`); explicit CAPTURE path may still use move fallback (`no_legal_attack_move_fallback`); telemetry propagated via `assault_sim/training_env.py`, `assault_sim/evaluation/evaluator.py`, `assault_sim/evaluation/results_analyzer.py` (`attack_fallback_to_move_count`, `attack_fallback_reason_counts`) | Pending smoke validation (`WAIT` controlled, reduced unconditional `ATTACK->ADVANCE`, no VP-conversion regression) |
| ATTACK micro-reposition quota guardrail | `assault_sim/decision/option_executor.py` + `assault_sim/decision/option_executor/combat.py`: per side/turn cap for no-legal-attack tactical reposition fallback, combined with survivability gate (terrain/pressure gain) before consuming budget | Pending smoke validation (`WAIT` reduction without `ATTACK->ADVANCE` rebound, stable damage ratio) |
| ATTACK micro-reposition dynamic quota by VP pressure | `assault_sim/decision/option_executor.py` uses a shared per side/turn fallback slot whose limit is widened in near-VP context (v8: near cap 3, far cap 1), and `assault_sim/decision/option_executor/combat.py` supplies near-VP detection (`nearest_vp_d <= 3`) before budget check/consumption | Pending smoke validation (recover VP conversion while preserving bounded `ATTACK->ADVANCE`) |
| ATTACK no-shot fallback objective/contact prioritization (v9) | `assault_sim/decision/option_executor/combat.py`: `_execute_attack()` now accepts fallback reposition on objective-ring progress (`no_legal_attack_objective_push`) or one-turn contact setup proxy (`no_legal_attack_contact_setup`), and applies bounded near-VP objective push (`no_legal_attack_forced_objective_push`) before hold when progress exists | Pending smoke validation (improved VP conversion/damage with bounded coercion and controlled `WAIT`) |
| ATTACK no-shot fallback legal follow-up attack gate (v10) | `assault_sim/decision/option_executor/combat.py`: `_attack_reposition_has_legal_followup_attack()` simulates post-move unit position and recomputes legal actions; `_execute_attack()` prioritizes fallback reposition that yields real legal follow-up attack (`no_legal_attack_legal_followup_setup`) before weaker proxies | Pending smoke validation (material VP/damage lift vs v9 with stable `ATTACK->ADVANCE` and `forced_ratio`) |
| ATTACK no-shot near-VP hard gate (v11) | `assault_sim/decision/option_executor/combat.py`: in near-VP context, `_execute_attack()` accepts fallback reposition only if it yields legal follow-up setup or objective progress; pressure/contact-only fallback is rejected there, then strict objective push is attempted before hold | Pending smoke validation (higher quality near-VP fallback, lower drift, measurable VP/damage lift vs v10) |
| ATTACK no-shot near-VP legal hard gate + diagnostics (v12) | `assault_sim/decision/option_executor/combat.py`: near-VP fallback now requires legal-followup plus progress signal for reposition acceptance; strict objective push also requires legal follow-up; otherwise emits diagnostic hold reason `no_legal_attack_no_followup_near_vp` | Pending smoke validation (verify legal-followup path activation, bounded drift, improved conversion without blind reposition) |
| ATTACK no-shot legal-followup failure attribution (v13) | `assault_sim/decision/option_executor/combat.py`: `_attack_reposition_legal_followup_status()` returns `(ok, status)` and near-VP hold emits `no_legal_attack_no_followup_near_vp:<status_context>` to distinguish failure roots (`deepcopy_failed`, `sim_actions_failed`, `no_attack_actions_after_move`, objective-push availability/progress issues) | Pending smoke validation (identify dominant failure mode and use it to target next behavior change) |
| ATTACK near-VP fire-window fallback (v14) | `assault_sim/decision/option_executor/combat.py`: `_attack_reposition_improves_fire_window()` allows bounded objective-progress fallback when legal follow-up is unavailable but fire-window geometry improves (`<=3` and distance reduction); new reasons: `no_legal_attack_fire_window_setup`, `no_legal_attack_forced_fire_window_push` | Pending smoke validation (lift VP conversion/damage from geometric setup while keeping coercion bounded) |
| Anti-zigzag non-RETREAT movement stabilization (v15) | `assault_sim/decision/option_executor.py`: in `execute()`, `ADVANCE`/`FLANK` actions are post-checked for immediate reversal (`_is_reversal_move`) under low-threat+objectives-pending; non-displacing movement outcomes are normalized to explicit `WaitAction` fallback (`non_displacement_move_wait_fallback`) instead of replacement movement synthesis | Pending Human-vs-AI validation (reduced visible zig-zag while maintaining predictable turn progression semantics) |
| Retreat pressure guardrail in low-threat objective context (v16) | `assault_sim/decision/option_executor.py`: in `execute()`, `RETREAT` is blocked under objectives-pending + non-emergency + low local threat (`_enemy_count_within(radius=2) <= 1`), remapped to `ADVANCE`; override reason `retreat_blocked_low_threat_objective_pressure` | Pending Human-vs-AI validation (prevent turn-3 passive retreat drift while preserving tactical withdrawals under real pressure) |
| Turn-start first-player anchor stability (v17) | `assault_model/runtime/game_state_runtime.py`: `RuntimeGameState` stores `first_player_side` at initialization and uses `_start_side_for_new_turn()` in both `next_activation()` rollover and `start_turn()` so each new turn re-anchors to the same first player when available (deterministic fallback otherwise) | Pending Human-vs-AI validation (no skipped AI first activation after turn rollover; stable turn ownership sequencing) |
| Win-rate-oriented PPO+reward retune (v45) | `assault_sim/config/train_config.json`: `sb3_n_steps=1024`, `sb3_n_epochs=5`, `sb3_gamma=0.997`, `sb3_ent_coef=0.0015`, `sb3_clip_range=0.12`, `sb3_learning_rate=8e-5`; `assault_sim/config/reward_config.json`: `vp_delta_weight=13.0`, `capture_vp_hold_streak_bonus=0.70`, `win_bonus=6.0`, `lose_penalty=6.0` | **Pending Validation**: run multi-seed A/B vs frozen baseline (`42/43/44`) and accept only if `true_win_rate` improves without material `loss_rate` regression |
| PPO/reward anti-plateau retune (v4) | `assault_sim/config/ppo_config.py` (`ENTROPY_COEF=0.07`) + `assault_sim/config/reward_config.json` (`hold_non_attack_penalty=0.36`) to increase exploration and disincentivize passive no-progress hold loops during training | Pending train/eval validation (break v1/v2/v3 metric equivalence and improve conversion/damage without coercion rebound) |
| Opening-turn anti-retreat pressure lock (v18) | `assault_sim/decision/option_executor.py`: in `execute()`, `RETREAT` is blocked when objectives remain pending, no capture emergency is active, and `turn <= 3`; remapped to `ADVANCE` with reason `retreat_blocked_opening_turn_pressure` | Pending Human-vs-AI validation (no early-turn disengagement in turn 2 while preserving emergency withdrawals) |
| Objective shortfall reward shaping (v19) | `assault_sim/rewards/progressive_reward.py`: tracked-side objective-rule episodes apply per-step VP-control shortfall penalty against configurable capture target ratio, plus extra terminal shortfall penalty on objective defeat; config knobs in `assault_sim/config/reward_config.py` + `assault_sim/config/reward_config.json` (`objective_control_target_ratio`, `objective_shortfall_step_penalty`, `objective_shortfall_terminal_penalty`) | Pending train/eval validation (`vp_entry_conversion_rate`, `captured_final_counts`, `loss_rate`, earlier VP-entry pressure) |
| RL action-finalization parity for train/eval (v20) | `assault_sim/envs/gym_assault_env.py` (`_GymActionController._finalize_rl_action`, `_catalog_priority_action`) and `assault_sim/evaluation/eval_sb3.py` (`SB3EvalController._finalize_rl_action`, `_catalog_priority_action`): enforce `ActionCatalog` legality at execution point, reject non-displacement pseudo-moves, and prioritize immediate legal uncaptured-VP step-in during fallback | Pending validation on smoke train+eval (`rl_training_finalized_reason` / `rl_eval_finalized_reason`, reduced invalid/non-displacement fallback reasons, improved VP step-in conversion) |
| Multi-turn mission planner orchestration (v21) | `assault_sim/decision/mission_planner.py` provides `MissionPlanner`/`PlannerContext`; runtime and train/eval controllers inject planner context into tactical execution (`assault_backend/main.py`, `assault_backend/services/sb3_ai_service.py`, `assault_sim/envs/gym_assault_env.py`, `assault_sim/evaluation/eval_sb3.py`, `assault_sim/decision/option_executor.py`) | Pending validation on smoke + multi-seed eval (`plan_commit_rate`, `focus_switch_rate`, `plan_stage_counts`, `plan_replan_reason_counts`, stable win/loss) |
| R2.1 learning-side invalid/fallback reduction (v22) | `assault_sim/rewards/progressive_reward.py` uses `action_finalized_reason`/`plan_stage` shaping; config knobs in `assault_sim/config/reward_config.py` + `assault_sim/config/reward_config.json`; KPI aggregation in `assault_sim/evaluation/evaluator.py` and `assault_sim/evaluation/results_analyzer.py` (`invalid_action_rate`, `fallback_rate`, `wait_recovery_sb3_backstep_rate`) | Pending validation on train+eval compare-gate (all three rates down vs baseline, no material `loss_rate` degradation) |
| Planner tagging semantic normalization (v23) | `assault_sim/decision/option_executor/state.py` (`_plan_intent_alignment_label`) + `assault_sim/decision/option_executor.py` (`_plan_unit_role`, `_tag_action`): treat `SETUP_CAPTURE` as CAPTURE-compatible for alignment and reduce role-tag `UNKNOWN` fallback in non-emergency maneuver contexts | Pending validation on smoke eval (`intent_commitment_rate_stub` up, `plan_role_counts_stub.UNKNOWN` down) |
| Planner intent-aware tactical steering (v24) | `assault_sim/decision/option_executor.py` (`execute`): planner-managed branch now uses `planner_intent` to bias strategy/option selection in `SETUP` (`CAPTURE`/`SETUP_CAPTURE` avoid passive options; `ATTRIT` avoids retreat default) before existing guardrails | Pending validation on smoke+multi-seed (`capture_intent_persistence`, `vp_entry_conversion_rate`, `loss_rate`) |
| Team Intent + Role Assignment activos (v25 / P4.2) | `assault_sim/decision/mission_planner.py` (contextual team intent from VP balance + local pressure), `assault_sim/decision/role_mapper.py` (per-activation role mapping using unit type + tactical state), `assault_sim/decision/option_executor.py` (L2 weighted scoring by role/intent + explicit `plan_fallback_reason`), and telemetry propagation in `assault_sim/training_env.py`, `assault_sim/evaluation/record_sb3_trace.py`, `assault_sim/evaluation/evaluator.py`, `assault_sim/evaluation/results_analyzer.py` | Pending validation (`strategy_stuck_ratio` down, `vp_entry_missed_rate` down, non-degrading `true_win_rate/loss_rate`, complete `record_sb3_trace` plan fields) |
| P4.2 retune against stuck/missed (v25-b) | `assault_sim/decision/option_executor.py` (`_plan_unit_role`, `execute`): deterministic anti-`UNKNOWN` role fallback, stagnation rotation `CAPTURE<->DENY` on planner blocked streaks, and near-VP CAPTURE preference for `ADVANCE` over `ATTACK` | Pending smoke validation (`plan_role_counts_stub.UNKNOWN` collapses, `strategy_stuck_ratio`/`vp_entry_missed_rate` down, stable `position_reversal_rate`) |
| P4.2 stabilization rollback (v25-c) | `assault_sim/decision/option_executor.py` (`execute`, `_tag_action`): removes blocked-step `CAPTURE<->DENY` rotation from v25-b after NO-GO regression and enforces last-mile anti-`UNKNOWN` role fallback during tag emission | Pending smoke validation (`plan_role_counts_stub.UNKNOWN` down; recovery of `loss_rate` and `position_reversal_rate` vs v25-b) |
| P4.3a budget observability contract (v26) | `assault_sim/decision/option_executor/state.py` + `assault_sim/decision/option_executor.py`: add per-turn budget violation counter and tag budget remaining/violation; `assault_sim/training_env.py` + `assault_sim/evaluation/record_sb3_trace.py`: propagate `plan_budget_remaining_by_role`, `plan_budget_violation_count`, `plan_budget_violation_delta`; `assault_sim/evaluation/evaluator.py` + `assault_sim/evaluation/results_analyzer.py`: compute/report `budget_compliance_rate` and `budget_violation_rate` | Pending smoke validation (budget metrics populated in trace/analyzer while tactical primaries remain stable) |
| Pre-train telemetry sharpening (v27) | `assault_sim/decision/option_executor.py`: annotate `rl_plan_role_unknown_reason` and `rl_capture_branch`; `assault_sim/training_env.py` + `assault_sim/evaluation/record_sb3_trace.py`: propagate role/capture diagnostics; `assault_sim/evaluation/evaluator.py` + `assault_sim/evaluation/results_analyzer.py`: aggregate `plan_role_unknown_reason_counts`, `capture_branch_counts`, `near_vp_l2_transition_counts`, and first-contact/progress/capture latency delays | Pending smoke validation (new diagnostics populated and used for R2.1 pre-training tuning) |
| R2.1-a near-VP conversion shaping (v28) | `assault_sim/rewards/progressive_reward.py` + `assault_sim/config/reward_config.py/json`: add CAPTURE near-VP no-conversion ADVANCE penalty and post-contact progress-move bonus; `assault_sim/evaluation/evaluator.py`: constrain first-progress timing to post-contact progress | Pending smoke validation (`vp_entry_missed_rate` trend down without tactical primary regressions) |
| Latency telemetry ordering cleanup (v29) | `assault_sim/evaluation/evaluator.py` + `assault_sim/evaluation/results_analyzer.py`: enforce valid delay ordering (`contact<=progress<=capture`) and publish invalid/missing latency counters to avoid false-negative interpretations | Pending smoke validation (latency outputs free of impossible negative delays, counters populated) |
| R2 helper 1 reward component logging (v30) | `assault_sim/rewards/progressive_reward.py` exposes last-step reward components; `assault_sim/training_env.py` propagates them in `info`; `assault_sim/evaluation/evaluator.py` + `assault_sim/evaluation/results_analyzer.py` aggregate and print `reward_component_means` for tuning attribution | Pending smoke validation (component means visible and actionable before multi-seed retunes) |
| R2.1-b retune-1 capture priority recovery (v31) | `assault_sim/config/reward_config.py/json` + `assault_sim/rewards/progressive_reward.py`: reduce `trade_weight`, add `non_capture_near_vp_penalty`, and add `capture_support_fire_window_bonus` for support/indirect units under CAPTURE post-contact contexts | Pending multi-seed validation (`vp_entry_missed_rate` down, CAPTURE share recovered, no reversal instability regression) |
| R2.1-b retune-2 conservative rollback (v32) | `assault_sim/config/reward_config.py/json`: restore `trade_weight` to `0.80`, keep `non_capture_near_vp_penalty`, and reduce `capture_support_fire_window_bonus` by 50% to avoid tactical over-correction from v31 | Pending multi-seed validation (`42/43/44`, 10 eps each): `vp_entry_missed_rate` down, `strategy_stuck_ratio` non-increasing, stable `loss_rate` |
| R2.1-b short multi-seed decision (v34 package) | Multi-seed short validation (`42/43/44`, 10 eps each) completed using reports `metrics_sb3_report_20260622T141425Z.json`, `metrics_sb3_report_20260622T141446Z.json`, `metrics_sb3_report_20260622T141506Z.json`; result remains mission-weak (`vp_entry_missed_rate` high, `strategy_stuck_ratio` high, `true_win_rate=0`) | **Pending Validation (decision checkpoint): NO-GO operational decision accepted for R2.1-b; requires formal R2.1-c confirmation run (`42/43/44`, 20 eps each) before final closure** |
| R2.1-c formal multi-seed confirmation (v34 package) | Formal confirmation run (`42/43/44`, 20 eps each) completed with reports `metrics_sb3_report_20260622T142012Z.json`, `metrics_sb3_report_20260622T142123Z.json`, `metrics_sb3_report_20260622T142209Z.json`; mission conversion remains blocked (`vp_entry_missed_rate` high, `objective_delta_term` inactive) and primary outcomes remain weak (`true_win_rate=0`, high `loss_rate`) | **Pending Validation (formal checkpoint): NO-GO accepted; next cycle constrained to single-lever reward tuning (`R2.1-d`, `vp_delta_weight` only), planner unchanged** |
| R2.1-d single-lever tuning kickoff (v35) | `assault_sim/config/reward_config.py/json`: increase only `vp_delta_weight` from `10.0` to `12.0`; no planner/guardrail or other reward-weight changes in this cycle | **Pending Validation: short train + multi-seed eval (`42/43/44`, 10 eps each) must show mission-direction lift before adding any second lever** |
| Eval source-mix observability for hybrid control (v36) | `assault_sim/evaluation/evaluator.py` + `assault_sim/evaluation/results_analyzer.py`: mission aggregates now expose `source_mix_counts/rates` (`sb3_kept`, `planner_override`, `finalizer_override`) and `source_mix_capture_event_rates` to quantify how often evaluation follows SB3 vs override layers | **Pending Validation: next eval report must show non-empty source-mix metrics and support diagnosis of override dominance before further reward retunes** |
| Eval minimal-overrides diagnostic flag (v37) | `assault_sim/evaluation/eval_sb3.py`: add CLI flag `--diagnostic-min-overrides` to disable planner-like eval coercions (step-in forcing and mission-priority CAPTURE forcing) while preserving legal finalization/fallback and report metadata (`diagnostic_min_overrides`) | **Pending Validation: paired eval runs (default vs diagnostic) must clarify how much mission performance depends on coercion vs SB3-kept decisions** |
| SB3 models workspace partitioning and side-scoped cleanup (v38) | `assault_sim/config/train_config.py/json`: new `sb3_models_subdir`; `assault_sim/train/train_sb3.py`: artifacts written under `models/<subdir>` and cleanup restricted to configured `rl_sides`; `assault_sim/evaluation/eval_sb3.py`: model/vecnorm resolution reads from same subdir and reports `models_subdir` metadata | **Pending Validation: run parallel side training using distinct subdirs and verify no cross-deletion/collision of artifacts** |
| R2.1-d.1 legality-pressure reward retune (v39) | `assault_sim/config/reward_config.py/json`: raise only `invalid_action_finalization_penalty` (`0.20 -> 0.50`) to reduce finalizer-corrected invalid actions before next retrain cycle | **Pending Validation: next train+eval must show lower `source_mix_rates.finalizer_override` / `fallback_rate` with stable primary outcomes** |
| Runtime model routing by scenario+side (v40) | `assault_backend/services/sb3_ai_service.py`: model/vecnormalize lookup and cache key changed from side-only to `(scenario, side)`; resolves from `train_config` template `sb3_models_subdir_template` (`scenario_{scenario}/side_{side}`) with compatibility fallback to flat `models/` layout | **Pending Validation: runtime should pick scenario-specific checkpoints without manual config swaps** |
| Train/eval models-subdir template auto-resolution (v41) | `assault_sim/config/train_config.py` adds `resolve_models_subdir(scenario, side)`; `assault_sim/train/train_sb3.py` and `assault_sim/evaluation/eval_sb3.py` use it to write/read artifacts per scenario+side workspace automatically | **Pending Validation: per-scenario eval must consume the correct side/scenario checkpoint without setting `sb3_models_subdir` manually per run** |
| Eval configurable train-config path (v42) | `assault_sim/evaluation/eval_sb3.py` adds CLI flag `--config` and routes config loading through the provided file path, mirroring `train_sb3` behavior for per-run config isolation | **Pending Validation: evaluate two different config files sequentially without replacing `assault_sim/config/train_config.json`** |
| Eval objective-only win criterion (v43) | `assault_sim/evaluation/results_analyzer.py` computes `true_win_rate` from `tracked_result` (`Vittoria*`), `assault_sim/evaluation/eval_sb3.py` and `scripts/run_eval_parallel_configs.ps1` expose objective-labeled win fields in comparative outputs | **Pending Validation: objective win rate must match `tracked_result_counts` (Vittoria/Vittoria totale) on replayed eval reports** |
| Eval side-asymmetry diagnostics (v44) | `assault_sim/evaluation/evaluator.py` captures legal-action volume/latency (`avg_legal_actions_per_decision`, `mean_action_catalog_gen_ms`) globally and by side; `assault_sim/evaluation/results_analyzer.py` aggregates/prints; `scripts/run_eval_parallel_configs.ps1` reports in comparative table | **Pending Validation: multi-side eval must output non-empty by-side metrics before throughput/quality claims** |
| Canonical P4 metric names in reports (v44) | `assault_sim/evaluation/evaluator.py` + `assault_sim/evaluation/results_analyzer.py` expose aliases `intent_commitment_rate`, `role_diversity_index`, and mission KPIs `plan_progress_rate`, `coordination_gain` | **Pending Validation: report JSON contains canonical keys with numeric values in smoke eval** |
| Side-to-ownership mapping excludes neutral sentinel | `assault_model/state/game_state.py` -> `_build_side_ownership()` now maps sides only to controlled `HexOwnership` values (`SIDE_A`, `SIDE_B`), never `NONE`; prevents assigning a real side to neutral ownership and corrupting VP capture/winner metrics | Pending validation via `assault_model/tests/test_game_state_side_ownership.py` + smoke eval for VP/winner consistency |
| CAPTURE threatened-owned-VP defense helper (pre-gate disabled) | `assault_sim/decision/option_executor_capture.py` -> `_best_defend_owned_vp_action()` remains available as helper; `assault_sim/decision/option_executor.py` no longer invokes owned-VP defense in CAPTURE pre-gate after regression run | Pending redesign/validation before re-enabling (`first_us_vp_loss_turn`, global win/loss impact, forced-ratio impact) |
| Retreat anti-oscillation guardrail (Human-vs-AI stability) | `assault_sim/decision/option_executor.py` -> `OptionExecutor.execute()` (`RETREAT` branch) blocks immediate reversal retreat (`A->B->A`) under pending objectives + non-emergency + low close-threat, and falls back to ADVANCE; helper: `_enemy_count_within()` | Pending smoke validation (lower backtrack loops, fewer `retreat_reversal_blocked_low_threat`-eligible oscillations, stable VP pressure) |
