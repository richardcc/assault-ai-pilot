paths:
  run_root: runs
  voec_config: voec_sim/configs/voec_config.yaml
  muzero_config: /tmp/muzero_wsl_s3_e3.yaml

benchmark:
  num_workers: 1
  scenario_id: battaglia_cittadina_2_1
  seeds: [42, 43]
  max_steps: 30
  max_steps_override: 0
  mcts_simulations: 3
  mcts_c_puct: 1.5
  mcts_temperature: 0.45
  matchup_profiles: [muzero_selfplay]
