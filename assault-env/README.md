# assault-env

Minimal training environment wrapping assault-engine.

This project provides a minimal training environment built on top of
the assault-engine core. It is designed for simulation, reinforcement
learning and visualization experiments.